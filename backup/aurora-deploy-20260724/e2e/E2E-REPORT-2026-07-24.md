# Aurora 1.4.0 Revive — end-to-end report (2026-07-24)

## Verdict

**Pass for ship gate.** Live theme matches committed `1.4.0` cream-fix package. Chrome, homepage Revive order, CTA, newsletter, and FSE sources are correct. Remaining work is revision polish (a11y contrast on orange kickers, header density, Track A pack leftovers, blog “Field notes” copy).

## Theme update performed

1. Audited live vs local hashes for CSS/JS/templates/fonts — **MATCH**.
2. Packaged clean zip from `main`: `kk-aurora-revive-1.4.0-main-1.4.0-20260724.zip`  
   SHA256 `17160188a7c8399807a1218700921edd7703e944703dcc66c782afbde63a4519`
3. Uploaded as media **#12632**; one-shot snippet **#15** extracted into `kk-aurora` (now **inactive**).
4. Snippet **#14** (cream CSS only) remains **inactive**.
5. FSE: `front-page`, `header`, `footer`, `page` all `source: theme` (no Site Editor overrides fighting).

## Public readback

| Check | Result |
|---|---|
| `style.css` Version | `1.4.0` |
| Cream contrast supersede | present; `#090c11` count `0` |
| Boost CSS has `--revive-surface` / woven | yes (`607a63d9eb`) |
| Header CTA | `/services/` on all sampled routes |
| Woven marquee | present sitewide |
| Homepage revive sections | present |
| Field notes / Dispatch in chrome | absent on homepage |
| Beehiiv newsletter | present |

### Routes (HTTP 200)

`/`, `/about/`, `/work/`, `/speaking/`, `/services/` → `/generative-ai-services/`, `/photography/`, `/blog/`, `/contact/`, `/sponsor-deck/`

Raw JSON: `readback.json`

### “Field notes” string hits (not chrome)

- `/blog/` archive H1: “Field notes for the age of generative everything.”
- `/work/` body copy mentions “Field notes, talks, and experiments…”

These are Track A content leftovers, not theme chrome labels.

## Contrast (WCAG math)

| Pair | Ratio | AA normal |
|---|---:|:---:|
| Ink `#171310` on cream `#efe6d2` | 14.88 | Pass |
| Cream on ink CTA | 14.88 | Pass |
| Muted `#6b5f52` on cream | 5.00 | Pass |
| Accent `#d94a1f` on cream (kickers) | **3.42** | **Fail** |

Focus ring on header CTA did not show under programmatic focus (`outline: none`) — keyboard `:focus-visible` needs a cream-safe forced style in a revision.

## Visual QA notes

- Homepage reads as Revive cream/ink broadsheet at desktop.
- Inner pages inherit cream surface; some packs still show cyan rules / drop-cap SaaS leftovers (Contact cyan bar, Services card chrome).
- Mobile header: nav wraps to two lines after Photography was added — usable but dense.
- Duplicate “Skip to content” links observed in DOM (2).

## Cleanup optional

- Delete media **12631** (CSS zip) and **12632** (full theme zip) after retention window.
- Delete inactive snippets **#14** / **#15** in wp-admin, or leave deactivated.
- Purge PressCACHE / Atomic if a specific edge URL looks stale (Boost already rebuilt).

## Screenshots

See `../screenshots/` and browser captures from this session (home / services / contact / speaking).
