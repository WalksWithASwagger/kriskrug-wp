# Work Plan — 2026-07-19

**Status:** active runbook after the 2026-07-19 consolidation + Wave 1 delivery session.
**Does not authorize** any live WordPress write or theme deploy. Snapshots, slug/ID checks, and KK approval still apply. The `kk-aurora` theme reaches production only via a **manual wp-admin zip upload** (SSH/SFTP blocked).

Declared snapshot: [`CURRENT-STATE-2026-07-16.md`](CURRENT-STATE-2026-07-16.md).

---

## Done this session (do not redo)

**Repository consolidation** (main is clean: 1 branch, 1 worktree, 0 stashes, CI green):
- Fast-forwarded `main` to `origin/main`; adjudicated + discarded the stale `mcp-wordpress-remote.sh` Varlock regression (main's #437 version is better).
- Removed 33 merged local branches, 2 remote branches, 17 worktrees; dropped 2 stashes. Diverged work archive-tagged first, then those tags pruned after verification.
- Landed 2 untracked files via PRs #438 (`.env.schema`) and #439 (redesign batch record), both merged.
- Closed as resolved-with-evidence: **#382** (codex worktree sweep), **#368** (remote head kill), **#380** (primary checkout reconcile), **#428** (WP 7.0.2 sync).

**Wave 1 redesign** (`swarm/issue-*` → 6 PRs, all CI-green + MERGEABLE, awaiting KK merge):
| Issue | PR | Note |
|---|---|---|
| #404 remove Podcast from nav | #440 | footer link already existed |
| #405 marquee to bottom | #441 | |
| #406 marquee research | #445 | doc: keep LED, kill automation, one "now showing" line |
| #407 hero "doesn't load" | #442 | root cause = scrim, not a load failure; lightened scrim. Rehost-to-media still open |
| #408 broken pills | #443 | grid `align-self` default; 1-line shared fix, 0 `!important` |
| #409 compact Writing cards | #444 | 3-up, killed featured card, dropped excerpt; 1→3 above fold, net −54 CSS lines |

---

## Critical path: 3 decisions gate Wave 2

Do not start Wave 2 styling/copy until these resolve.

1. **#423 — Stylesheet: rebuild vs incremental.** Framed on the issue 2026-07-19 with audit numbers (6,219 lines / 5 files / 72 `!important` / doubled component layers). **Recommendation: B (incremental/boy-scout), C (targeted component-layer consolidation) as fast-follow.** Blocks all of Wave 2 styling.
2. **#422 — Primary CTA ("Book a keynote" too narrow).** Repeats across hero, newsletter, footer, and every page CTA. Decide once.
3. **#374 — Protect `main` + dependency security controls.** Before more swarm volume.

---

## Wave 2 — sequenced (draft-first, KK approval gate before publish)

**2A. Homepage sections** (after #423 + #422):
#410 hero tagline → #411 Join BC/Futureproof → #412 Creative Labs → #413 client logo soup → #414 speaking stages → #415 "What People Say" (needs verified 2024–2026 testimonials) → #416 newsletter rename → #417 footer bento.

**2B. Full pages** (reuse 2A components): #418 About → #419 Speaking → #420 Services → #421 Contact.

**2C. QA pass, last:** #424 site-wide hover/focus/interactivity + #127 mobile responsive QA.

---

## Parallel lanes (not gated on the redesign)

- **SEO/GEO:** #425 (publisher/news sitemap + article schema), #402 (surprising-search content), #383 (SEO decision batch — human gates).
- **Ops/indexing:** #426 (slow-burn indexing runbook).
- **Wave 1 follow-up:** #407 hero image rehost from the punkrockai.com hotlink into the kriskrug.co media library (separate live-media task).

---

## Deploy the Wave 1 PRs

1. KK reviews + merges #440, #441, #442, #443, #444, #445.
2. Agent runs `make aurora-package`, hands KK one theme zip + a per-issue verification checklist (logged-out at 375 / 768 / 1440).
3. KK uploads the zip via wp-admin, then Pagely purge + logged-out verification per issue.

---

## Guardrails

- One lane per commit/PR. Prefer theme OR content, never both in one PR.
- No live WP writes or theme deploys without explicit KK approval of the exact artifact/checklist.
- Preserve the concurrent media/photo workstream in the working tree (KK's) — do not stage or clean it.
