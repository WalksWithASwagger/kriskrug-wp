# Work Plan — week of 2026-07-25

> **STATUS: Historical.** Superseded by [`WORK-PLAN-2026-07-30.md`](WORK-PLAN-2026-07-30.md) + [`CURRENT-STATE-2026-07-30.md`](CURRENT-STATE-2026-07-30.md) + [`MASTER-PLAN-2026-07-30.md`](MASTER-PLAN-2026-07-30.md).

**Owner:** KK · **Generated:** 2026-07-25 (good-morning catchup + cleanup pass)
**State at start:** `main` synced to origin (was 67 behind, now current). Repo Aurora **1.4.6**. Live Aurora **1.4.3** (public `style.css` readback). No open PRs. ~24 open issues, dominated by the #403 redesign epic and a new 1.5.x refactor track.

This plan is Track-scoped per `TWO-TRACK-MODEL.md`: one lane per commit, don't mix Track A (content/SEO) and Track B (theme) in a single session.

---

## The one thing that matters most

**Live is two accessibility releases behind the repo.** 1.4.4→1.4.6 are tested (33/33 pass, visual-baseline harness in place) and shipping real WCAG contrast + skip-link + cream-system fixes. They help users *now* and nothing depends on holding them back.

→ **Deploy Aurora 1.4.6 to production, gated on KK approval + rollback path** (safety rule #1). This is the week's anchor.

---

## Decision gate that unblocks everything else

**#423 [DECISION] Stylesheet hierarchy: rebuild from the ground up vs incremental repair** (high).
The entire new 1.5.x refactor track is queued behind this call:
- #474 cascade layers + token scaffold (high) · #475 reset/base layer · #476 primitives + block-editor parity (high) · #477 one-component-per-PR epic · #478 dead-CSS delete · #479 12→3 breakpoints · #480 retire Track A page-CSS blocks · #481 kk- class rename

→ **KK-only decision. Make it early in the week** so refactor work can start. Nothing below Track B refactor should begin until #423 is answered.

---

## Day-by-day (week of 2026-07-25)

### Day 1 — Ship what's ready (Track B)
- [ ] **Deploy Aurora 1.4.6** to prod via `make aurora-package` → wp-admin zip upload → Pagely purge → logged-out public readback confirming `style.css` = 1.4.6. Rollback ref captured first. *(KK approval gate.)*
- [ ] Update `AGENTS.md` + `CURRENT-STATE` live-version line to 1.4.6 after readback (the doc still says live 1.4.3 / repo 1.4.4).
- [ ] Refresh morning-truth: newest committed report is 2026-07-16, nine days + 67 commits stale.

### Day 2 — Fix the visible bug (Track B)
- [ ] **#407 homepage hero image does not load correctly** (bug, high, wave-1). Reproduce against live, trace in `theme/kk-aurora/`, patch on a feature branch off fresh `main`, verify with theme-smoke + logged-out render. Ship as 1.4.7 or fold into the Day 1 deploy if timing lines up.

### Day 3 — Answer #423, then scaffold (Decision → Track B)
- [ ] KK rules on **#423**. Record the decision on the issue.
- [ ] If "incremental" or "rebuild-in-place": start **#474 cascade layers + token scaffold** — explicitly behavior-neutral, so it's the safe first step under either path. Verify no visual diff via the #473 baseline harness.

### Day 4 — Content wave-2, packet 1 (Track A)
Pick the highest-impact copy fix — all are `needs-human-review`, so draft-only until KK signs off:
- [ ] **#410 Hero tagline** — replace "Author judgment for generative everything" (high, fast, top-of-page). *or*
- [ ] **#416 Newsletter section** — rename, kill dispatch/field-notes cliché, add thumbnails, one CTA (high). *or*
- [ ] **#418 About page** — unify backgrounds/column widths, kill double "public trail" (high).
Draft in `content/drafts/`, KK review, then publish slug-matched (safety rule #2).

### Day 5 — Content wave-2, packet 2 + close the loop (Track A)
- [ ] Second content packet from the #411–#420 set (Creative Labs #412, client logo soup #413, Speaking #414/#419, What People Say #415, Services #420).
- [ ] Groom the backlog: close junk **#427 (probe)**; decide **tyee branch** (see below); confirm #424 interaction-audit fixes are tracked separately from its (done) audit half.
- [ ] `wind-down`: park state, commit lane-scoped, note where Day-1-of-next-week opens.

---

## Parked / needs a KK call (not this week's critical path)

- **`codex/tyee-coverage-publications` branch** — one unmerged `content(press): add Tyee AI adoption coverage` commit, but the branch is far behind main and its press assets already landed via #454. Kept during cleanup. **Decide:** cherry-pick the Tyee bit onto fresh `main`, or delete the branch as superseded.
- **#402** SEO authority hubs — enhancement, no wave assignment. Slot in only if content days run short.
- **#385** normalize WordPress workflow skill without breaking automation — tooling hygiene, low urgency.
- **#480 retire Track A page-content CSS** — depends on the #423 direction; don't start until the theme's cream system fully covers those six routes.

## Cleanup already done this session
- Fast-forwarded `main` 67 commits (0 local commits lost).
- Deleted merged branches: `codex/415-homepage-trust-identity` (#451), `codex/approved-community-photo-20260720` (#492), `codex/publications-editorial-archive` (#454), and their two worktrees under `/Users/kk/Code/agent-worktrees/`.
- Removed stray `err.tmp`.

## Guardrails (from AGENTS.md + incident 2026-05-15)
1. Rollback path before any destructive/live change; logged-out public readback proves deploys.
2. Slug-based idempotency for any Notion→WP publish; never PATCH without confirming the slug target.
3. One lane per commit; `main` is canonical for both tracks.
4. Never run the connector against prod without `--dry-run` first.
