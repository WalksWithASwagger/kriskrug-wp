# Work Plan — 2026-07-16

**Status:** active execution plan after the 2026-07-16 morning truth pass.  
**Supersedes for sequencing:** `POST-SHIP-AUDIT-WORKPLAN-2026-06-04.md` and the unmerged July-09 audit draft.  
**Does not authorize** any live WordPress write. Dry-run, slug/ID checks, snapshots, and KK approval still apply.

Declared snapshot values for drift checks live in [`CURRENT-STATE-2026-07-16.md`](CURRENT-STATE-2026-07-16.md).

## North star

1. Make declared truth match live (docs + secrets).
2. Land one production SEO lane: deploy Aurora, then run the approved #339 batch.
3. Keep Track A content packets body-only and one-per-session.

## Phase 0 — Truth + access

1. Land this dated CURRENT-STATE + front-door pointers (AGENTS / current-state README / Makefile defaults).
2. Commit `.env.schema` + `make env-check` (Varlock contract; no plaintext secrets).
3. KK adds **Cursor Cloud secrets** (or a process-env injection path): `WP_USER`, `WP_APP_PASSWORD`, optionally `NOTION_TOKEN`.
   - **Varlock on a laptop does not reach Cursor Cloud VMs.** Filling 1Password / Varlock locally is correct for local `varlock run`, but cloud agents still need the same values injected as Cloud secrets (or a service-account/`OP_TOKEN` path explicitly wired for this environment).
4. Re-run authenticated `make status-readonly` / draft-queue audit.

**Done when:** drift on open PRs/issues/WP version is clean against CURRENT-STATE-2026-07-16; authenticated draft counts are trustworthy.

## Phase 1 — Aurora deploy (#351 → 1.3.40)

Live is **1.3.37**. Repo is **1.3.40**. The hashed 1.3.39 zip from #351 does **not** include the 1.3.40 search-title module (#357/#358).

Fresh package prepared 2026-07-16 (see [`backup/aurora-deploy-20260716/DEPLOY-HANDOFF.md`](../../backup/aurora-deploy-20260716/DEPLOY-HANDOFF.md)):

- Deploy SHA-256: `8e1c1321f94b1caf5d899697f5ddef6256d1274c74a67cd64d84645b1c24fad5`
- Rollback SHA-256: `cfa1307e68db77c9bd8b9423fbd35be984a1d98b6d4f6829b3a540b701a1d1b4`

Recommended path:

1. KK uploads only after exact zip/checksum approval; purge PressCACHE; smoke `/`, `/blog/`, `/blog/page/2/`, `/about/`, `/speaking/`, one article (normal + crawler UAs).
2. Confirm search-title regression sample from `AURORA-SEO-TITLES-1.3.40-HANDOFF-2026-07-14.md`.
3. Close #36 / #346 / #347 / #357 companions only after public evidence.

**Out of scope:** #316 schema snippet deploy, #345 `blogname`, GSC indexing requests, content patches.

## Phase 2 — Measured July publisher batch (#339)

After Aurora is live and verified:

1. Refresh target IDs/slugs/modified dates; stop on stale guards.
2. Snapshot targets + options.
3. Apply approved SEO fields, then body-only patches (#249, #328, #335, #336, #342).
4. Authenticated + anonymous readback; schedule measurement windows.

Keep #331 (archive policy) and remaining #353 H1 routes as separate later lanes.

## Phase 3 — One Track A content packet

| Packet | Issues | Material on `main` |
|---|---|---|
| 3A Hub links | #278 / #284 | `internal-link-batch-plan-2026-07-07.md` + `fixes/issue-284-*` |
| 3B A11y → WP draft | #288 / #48 | `content/drafts/accessibility-statement-2026-07/` |
| 3C About/bio | #290 | about-bio source map + payload plan |
| 3D Media appearances | #95 | Human editorial gate first |

Default when SEO waits on KK: dry-run **3A**, or finish **3B** human gates then draft-only WP write.

## Phase 4 — Parked / later

- #331 taxonomy sitemap / indexability
- #353 remaining body-H1 routes (one target at a time)
- #274 GSC submit/monitor
- #125 / #86 / #127 Track B perf + mobile QA
- #318 repo bloat
- Vault `op()` wiring once local Varlock + 1Password are confirmed

## Stop rules

- No force-push to `main`; `allow_auto_merge=false`.
- No live REST/theme upload without exact KK approval of the artifact.
- Do not mix Track A content commits with Track B theme commits.
