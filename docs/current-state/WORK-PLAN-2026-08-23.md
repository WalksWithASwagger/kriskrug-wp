# Work Plan 2026-08-23: drain the merge queue, then apply the hub packs

**Status:** active day runbook. **Supersedes** [`WORK-PLAN-2026-08-17.md`](archive/WORK-PLAN-2026-08-17.md).
**Plan of record:** [`MASTER-PLAN-2026-07-30.md`](MASTER-PLAN-2026-07-30.md).
**Declared snapshot:** [`CURRENT-STATE-2026-07-30.md`](CURRENT-STATE-2026-07-30.md) (Makefile default; do not treat its PR/issue counts as live).
**Does not authorize** any live WordPress write. Dry-run, slug/ID checks, snapshots, and KK approval still apply.

---

## Verified state (2026-08-23, public readbacks)

| Signal | Value |
|---|---|
| WordPress | **7.0.4** (homepage generator) |
| Aurora live / repo | **1.6.9** / **1.6.9** (public `style.css` readback 2026-08-23; parity, no drift either direction) |
| Open issues | **44** |
| #826 taxonomy + 2819 contact href | **live** 2026-08-18, issue closed |
| #827 photography hub links | **live** 2026-08-18, issue closed. 1210 still unlinked (that is #828) |
| Origin besides `main` | open PR heads only (hub packs #865 to #868, this runbook, drafts #887 to #891) |

### Landed on `main` 2026-08-23

| PR | What |
|---|---|
| #870, #871 | `python-dotenv >=1.2.3` (root + `scripts/notion-to-wp`) |
| #872 | `github/codeql-action/upload-sarif` 4.37.6 to 4.37.7 |
| #864 | Archived three issue drafts whose children are closed. **Closed #744** (#339 stays open, as planned) |
| #873 | Deleted `customize-for-kriskrug.sh` and `monitor-agents.sh`. **Closed #742** |

## Merge lane: the queue cannot drain by review

This is the blocker that stalled #864 to #873 for four days, and it will stall every future agent PR the same way.

1. Every non-dependabot PR here is authored by **WalksWithASwagger**. Branch protection on `main` requires **1 approving review**, and GitHub does not let an author approve their own PR. There is no second reviewer on this repo.
2. The designed escape hatch, the **`agent-safe-merge`** workflow, fails on every run: `Repo secret AGENT_MERGE_TOKEN is missing.` It has never succeeded.
3. What does work today: `enforce_admins` is **false** and KK holds admin, so `gh pr merge --squash --admin` goes through. Dependabot PRs have a different author and can take a normal approve plus merge.

Required checks are `strict: true`, so every branch shows `BEHIND`. Check file overlap against `main` before an admin merge instead of assuming the stale base is safe.

**To fix the lane properly:** create a classic PAT with `repo` scope on an account with write access, add it as Actions secret `AGENT_MERGE_TOKEN`, and add the same value as Cursor Cloud secret `GH_TOKEN`. Until then, treat `agent-safe-merge` as broken and do not cite it as the sanctioned path.

## What does **not** need KK

Labeled `needs-human-review` or still open, but the remaining work is repo/ops, not a taste call:

| Issue | Why it can wait |
|---|---|
| **#738** | Origin remotes already swept. Leftover is laptop worktrees only. |
| **#749** | Local PNG prune on the laptop, not a Cloud VM. |
| **#706** | Script diet is live at origin. Open only for the leftover gtag / PSI note. |
| **#402** | Parent epic. Children exist. Do not resplit. |

Do **not** file more audit issues. Do **not** swarm #477 / #424 / #481 / homepage restyle.

## What **does** need KK (real gates)

| Item | Why you |
|---|---|
| Merge or admin-merge #865 to #868 | Hub link payloads, drafts only. See the merge lane section above. |
| Live apply #829, #830, #831, #832 | Packs are drafted. Still a live PATCH. Needs `WP_USER` + `WP_APP_PASSWORD` and an explicit go. |
| **#4** alt-text batches 0 and 1 | Applier and dry-run are on #891. Dry run is clean. Needs creds and your go. |
| **#828** rewrite of post 1210 | Only writing task in the #402 split. You own the voice. |
| **#731** Boost critical CSS | wp-admin regenerates. App password 403s the data store. |
| **#274** Search Console | Human only. |
| **#771 / #745 / #735 / #277** | Publish or park, cull, brand canon, contact form. Taste or legal. |
| **#767 / #709** | Host-admin and header rulings. |
| **#737** backup tree | Blast radius. Proposal is on `main`. |

## New since the 08-17 runbook

Two epics opened 2026-08-22 with wave-1 drafted in worktrees and open as draft PRs:

- **#876** Press kit v2, booking-first EPK. Wave 1: #877 copy deck (PR #888), #878 asset rights inventory (PR #887), #879 Aurora brand reference (PR #890). Blocked behind it: #880, #881, #882.
- **#883** AI use policy. Wave 1: #884 policy draft (PR #889). Blocked behind it: #885 payload plus contract tests, #886 publish.

Both publish steps are gated on KK. Neither wave-1 PR writes to live.

## Round next: Track A hub applies, then stop

1. Authenticated dry-run each pack (`--from-files` first, then live GET dry-run). Recut needles if public-rendered stand-ins miss `content.raw`.
2. `--apply` **#829**, then **#830**, then **#831**, then **#832**. Parallel-safe with each other now that #826 is live. Shared-post rule still: do not start **#834** until #829 (post 12030) and **#833** (post 11700) have landed.
3. Do **not** apply #480 photography.html after #827. It would wipe the live hub links unless recut.
4. Then **#833** (MBO cluster) payload if it is not already drafted. Then **#834**. Then **#828** only with KK copy.

## Gate 2: still do not touch

Homepage copy confirm on the 1.6.9 labs list, speaking W2/W3 (#640/#641), `#481` class rename, `#477` component epic, `#424` sitewide hover pass, more visual-baseline PNGs.

## No new roadmap issues

The product holes already have issues. Sequencing, not filing:

- Track A: finish #402 children, then the leftover #339 publisher rows that are still unapplied.
- Track B: #403 is the umbrella; live residue is #424 (and later #477). Do not cut a new redesign epic.
- SEO ops: #331 sitemap policy, #274 GSC. Both human.

## Launch prompt (hub apply, credentialed session)

> #826 and #827 are live. Dry-run then apply #829, #830, #831, #832 per their APPLY.md. Snapshot-first, slug/ID check, no extra copy. Recut find needles if rendered stand-ins miss raw. Then stop for readback.

---

**Last verified:** 2026-08-23. Live `style.css` **1.6.9**, repo **1.6.9**. #870 / #871 / #872 / #864 / #873 merged today; #742 and #744 closed with them. Next: land #865 to #868, then hub `--apply` when KK says go. Run `make status-readonly` and a public `style.css` readback before acting.
