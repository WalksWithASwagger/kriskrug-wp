# Work Plan — week of 2026-07-26

**Author:** morning session (KK + Claude Code). **Supersedes** `WORK-PLAN-2026-07-16.md` as the active day/week runbook. Read alongside the newest `reports/morning-truth-*.md`.

## Where things stand (verified 2026-07-26)

- **Live theme:** Aurora **1.4.8** (deployed 2026-07-25 via SFTP pipeline).
- **Repo `main`:** Aurora **1.4.9** — the #416 homepage newsletter refresh (PR #505) merged this morning but is **not yet deployed**. Main is one version ahead of live.
- **PR #493** (`theme/474-cascade-layers-scaffold`): Aurora **1.5.0** cascade-`@layer` scaffold. Green CI, **held on the pixel gate (#494)**. Now conflicts with main (1.4.9) → needs a rebase.
- **Pixel gate (#494):** baseline frozen by a cloud pod — run `20260726T194734Z`, live 1.4.8, 33 PNGs. The diff can only run **post-deploy** (it compares live-vs-live) and only in the cloud pod (Playwright harness). The Mac can deploy (Keychain SFTP); the pod can diff. Split capability.
- **Branch hygiene:** 46 dead refs pruned + 19 merged branches deleted this morning. Remaining: `theme/474` (PR #493), `cursor/494-pixel-gate`, and 3 stale `codex/*` WIP branches awaiting a keep/kill call.

## The keystone (do this first — it unblocks 7 issues)

**Land Aurora 1.5.0 through the pixel gate.** #475–#481 are all `blocked` behind #493.

Recommended sequence (one deploy carries both the newsletter and the scaffold):

1. **Rebase #493 onto main (1.4.9)** → branch becomes scaffold **+** newsletter at 1.5.0. (Cloud agent, it holds the harness. Keep 1.5.0 as the version.)
2. **KK decision gate:** approve deploying behavior-neutral **1.5.0** to prod. Rollback zip (1.4.8) is already staged at `backup/aurora-deploy-20260726/`; rollback = SFTP name-swap.
3. **Deploy 1.5.0** — `scripts/notion-to-wp/.venv/bin/python scripts/deploy_theme_sftp.py deploy` (Mac only; Keychain).
4. **Cloud pod runs** `make visual-diff BASE=20260726T194734Z`. Pass = zero diff **everywhere except the intended newsletter band** (that region changed by design via #505; adjudicate the diff report, don't expect pure-zero).
5. Any non-newsletter flip → fix **only** in `theme/kk-aurora/assets/css/09-late.css` (unlayered, no new `!important`).
6. **Merge #493.** Now live = main = 1.5.0. Verify logged-out `style.css?cb=` header + Pagely purge if needed.

*Tradeoff noted:* bundling the newsletter into the 1.5.0 deploy means the scaffold's "zero-diff" proof is adjudicated (exclude the newsletter region) rather than pure-zero. Acceptable given 1.5.0 is behavior-neutral and this saves a second prod deploy. If you'd rather have a pristine scaffold proof, deploy scaffold-only first, then newsletter second (two deploys).

## Then: the rebuild chain (Track B, sequential, each its own PR + gate)

Once #493 lands, these unblock in order — one component/layer per PR, each pixel-gated:

- **#475** — reset + base layer; retire/opt-in the drop cap (1.5.1)
- **#476** — primitives layer + block-editor parity (1.5.2)
- **#477** — component migration epic, one component per PR (1.5.3+)
- **#478** — delete dead CSS; fold animations + bleeding-edge into the stack (1.6.0)
- **#479** — consolidate 12 breakpoints → 3-step scale
- **#480** — retire Track A page-content CSS blocks (six live routes)
- **#481** — rename `aurora-`/`revive-`/`kkm-` → single `kk-` convention (1.6.1)

## Parallel: content wave (Track A, fan out as draft-PR agents)

Independent of the theme chain — safe to run concurrently.

- **#496–#500** — Futureproof Festival announcement post (epic + 4 waves; lineup, story in KK voice, assets/alt-text, WP draft — **no publish** without KK).
- **#411–#420** — homepage section redesigns (Join BC/Futureproof, Creative Labs, logo soup, speaking stages, What People Say, Services, Speaking, About). All `needs-human-review`.
- Rule: draft only, dry-run → slug-match → publish; KK reviews before anything goes live.

## Cleanup + ops (small, do between big blocks)

- **#369 reclaim** — approve `docs/current-state/RECLAIM-LIST-2026-07-24.md`, then execute safe deletions. **Guardrail: never touch untracked files** (KK's local drafts). Nothing deleted without the ranked list signed off.
- **3 stale `codex/*` branches** (`415-homepage-trust-identity`, `approved-community-photo-20260720`, `publications-editorial-archive`) — 3–6 days old, unmerged, no PR. KK: salvage or delete? Unproven, so left intact for now.
- **Deploy the 1.4.9 newsletter** if the keystone sequence slips — main is ahead of live; the homepage refresh isn't public until a deploy.

## KK decision queue (blocks the above)

1. **Approve prod deploy of 1.5.0** for the pixel gate (keystone step 2). ← highest leverage
2. **One vs two deploys** for scaffold+newsletter (recommendation: one).
3. **#369 reclaim list** — approve/trim.
4. **3 stale codex branches** — keep or kill.

## Success criteria for the week

- [ ] #493 merged; live = main = Aurora 1.5.0; newsletter refresh public.
- [ ] At least #475–#476 of the rebuild chain landed and gated.
- [ ] Futureproof post staged as a WP draft (KK-review-ready, unpublished).
- [ ] #369 reclaim executed against the signed-off list.
- [ ] Branch list stays lean (only active-PR + WIP branches remain).
