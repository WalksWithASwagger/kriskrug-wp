# Work Plan 2026-08-24: finish the reviewed alt-text lane, then resume hub applies

**Status:** active two-session runbook. **Supersedes**
[`WORK-PLAN-2026-08-23.md`](WORK-PLAN-2026-08-23.md).
**Plan of record:** [`MASTER-PLAN-2026-07-30.md`](MASTER-PLAN-2026-07-30.md).
**Declared snapshot:** [`CURRENT-STATE-2026-07-30.md`](CURRENT-STATE-2026-07-30.md)
(Makefile default; use `make status-readonly` for live counters).
**Does not authorize** a live WordPress write. Each live lane still requires
explicit KK approval, a dry run, identity checks, a private snapshot, and
exact readback.

---

## Goal and success criteria

Finish the already-reviewed Batch 3 media-alt lane without mixing it with the
SEO hub payloads, then return to the hub queue with issue #4 in a measured,
decision-ready state.

Success means:

- the Batch 3 draft inventory is merged before any live apply;
- every approved attachment is selected by exact media ID and verified by
  ID plus filename before write;
- the first ordinary item and the first NCR-bearing item both have exact
  post-write authenticated readback;
- every apply has a private mode-0600 pre-write snapshot;
- a fresh 216-route recount completes without silently accepting fetch gaps;
- issue #4 records the measured delta and the remaining scope decision;
- #829-#832 remain untouched until the alt-text session is closed or parked.

## Verified current state (2026-08-24)

`make status-readonly` completed with all data sources available:

| Signal | Value |
|---|---|
| Open PRs / issues | **0 / 43** |
| WordPress | **7.0.4** |
| Aurora live / repo | **1.6.9 / 1.6.9**, parity |
| Draft posts / pages | **66 / 4** |
| Worktrees | canonical `main` only |
| `origin/main...main` | `0 0` before this closeout |

Issue #4 execution state:

- Media 6835 and 12646 were applied and verified on 2026-08-24.
- All 34 Batch 1 in-content rows across seven pages were applied one page at
  a time and independently verified as `already-applied`.
- Inventory Batch 3 now has reviewed strings for **76 rows / 75 unique media
  IDs**. No Batch 3 live write has been made.
- Media 12597 passed the requested scoped dry run: credentials resolved,
  ID and filename matched, live alt was empty, and status was `would-write`.
- The media applier's broad selector now sees 77 unique targets: the two
  already-applied IDs plus the 75 Batch 3 IDs. Use `--only-media-id`; do not
  use broad `--batch media --apply` for this lane.
- Inventory batches 4-6 remain parked at roughly 1,070 archive images.

## Options and recommendation

1. **Finish Batch 3 next (recommended).** Preserves visual-review context,
   closes the highest fixes-per-write slice, and gives issue #4 a clean
   measurement checkpoint. Tradeoff: hub applies wait two sessions.
2. **Switch immediately to #829-#832.** Advances authority-hub SEO sooner,
   but leaves an approved, verified accessibility lane half-finished and
   increases restart risk.
3. **Pause all live work for archive triage.** Lowest mutation risk, but it
   delays both ready lanes and does not improve current public output.

Choose option 1. Keep each session Track A-only and stop before starting the
hub payloads.

## Session 1: Batch 3 canary, maximum five attachments

### Approval gate

KK must explicitly approve the Batch 3 live canary. Approval to merge this
plan or its inventory is not approval to write WordPress.

### Steps

1. Run `make doctor`, `make status-readonly`, pull current `main`, and confirm
   the Batch 3 inventory commit is present.
2. Re-run the dry run for media 12597. Confirm ID `12597`, filename
   `spark_3d_andre.jpg`, empty current alt, and the exact proposed string.
3. Apply only media 12597 with `--only-media-id 12597 --apply`. Inspect the
   snapshot mode and exact authenticated readback; then re-run its dry run and
   require `already-applied`.
4. If and only if the canary is exact, repeat one at a time for media 12593,
   12541, 12536, and 12528. Stop after five total writes.
5. Record report paths, statuses, and any conflict. Do not begin the remaining
   70 IDs in this session.

### Stop conditions

- ID or filename mismatch;
- existing non-empty alt that differs from the draft;
- missing or non-private snapshot;
- post-write readback mismatch;
- repeated network failure or a public/authenticated disagreement that cannot
  be explained by cache;
- any request to broaden the live scope beyond the five named IDs.

### Acceptance

- Five or fewer explicitly named writes.
- Every written item has `written-verified`, a mode-0600 snapshot, and a
  follow-up `already-applied` dry run.
- Zero conflicts, identity refusals, or unreviewed strings.

## Session 2: complete Batch 3, recount, and update issue #4

### Approval gate

Continue only if Session 1 is clean and KK explicitly approves the remaining
Batch 3 IDs. A clean canary is evidence, not authorization.

### Steps

1. Refresh `main`, run doctor/status, and verify the five canary IDs are
   `already-applied`.
2. Apply the remaining Batch 3 IDs one attachment at a time. On the first
   NCR-bearing item, media 5024, stop and require exact authenticated readback
   of the numeric character reference before continuing.
3. Run a complete authenticated Batch 3 dry run. Require all 75 unique Batch 3
   IDs to report `already-applied` and the two earlier IDs to remain exact.
4. Run `recount_live.py` across all 216 routes. Retry transient failures; do
   not publish a total from a partial crawl.
5. Add one evidence comment to issue #4 with before/after counts, report paths,
   conflicts, and the still-open decisions below. Do not close #4.

### Acceptance

- All 75 unique Batch 3 attachments are exact or explicitly reported as
  skipped; no silent partial success.
- First NCR-bearing write has exact authenticated readback.
- Recount covers 216/216 routes.
- Issue #4 has a current measured status comment.
- No #829-#832 payload is applied in the same session.

## What remains after these two sessions

1. **Issue #4 scope decision:** do all, traffic-prioritize, or permanently
   park inventory batches 4-6 (~1,070 archive images).
2. **One known Batch 2 in-content row:** page 6815 still needs its baked
   `post_content` alt handled separately from media 6835.
3. **`/home/` disposition:** redirect, unpublish, or keep and maintain.
4. **Fresh tracking-pixel classification:** the 2026-08-02 inventory counted
   216 missing-alt Meta pixels, while the later script-diet readback removed
   pixel markers on sampled routes. Trust the new full recount, not either old
   blanket claim.
5. **Issue-level acceptance:** screen-reader testing and WCAG 2.1 AA
   verification remain open even after the mechanical backfill.
6. **Next Track A lane:** apply-ready hub packs #829, #830, #831, and #832,
   each snapshot-first after separate KK approval. Then #833 before #834;
   #828 remains a KK voice decision.
7. **Operational backlog:** #740 archives superseded plans; #738/#749 are
   laptop cleanup; #737 is the remaining backup-tree blast-radius decision.

## Exact next-session prompt

> Read WORK-PLAN-2026-08-24.md and the alt-text APPLY-RUNBOOK. Run doctor and
> status-readonly. Confirm the Batch 3 inventory is on main. Ask KK for explicit
> approval of the five-ID canary. If approved, dry-run then apply only 12597,
> verify its private snapshot and exact authenticated readback, re-dry-run for
> already-applied, then repeat one at a time for 12593, 12541, 12536, and 12528.
> Stop after five writes and report; do not touch #829-#832.

---

**Last verified:** 2026-08-24. Live WordPress 7.0.4; Aurora live/repo 1.6.9;
open PRs 0; open issues 43. Runtime counters remain readbacks, not promises.
