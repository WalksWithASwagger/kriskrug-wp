# Work Plan — 2026-08-09

**Status:** active day/week runbook. **Supersedes** `WORK-PLAN-2026-08-05.md` (deploy window never opened; gap widened to three versions; new critical repo-integrity lane).
**Plan of record:** [`MASTER-PLAN-2026-07-30.md`](MASTER-PLAN-2026-07-30.md).
**Declared snapshot:** [`CURRENT-STATE-2026-07-30.md`](CURRENT-STATE-2026-07-30.md) (counts drifted — see verified state below).
**Does not authorize** any live WordPress write. Dry-run, slug/ID checks, snapshots, and KK approval still apply.

---

## Verified state (2026-08-10 03:43 UTC session, local date 2026-08-09)

| Signal | Value | Source |
|---|---|---|
| Open PRs | **0** | `gh pr list` |
| Open issues | **44** | `gh issue list --json number \| length` |
| Aurora live | **1.5.9** | `curl kriskrug.co/.../kk-aurora/style.css` readback |
| Aurora repo | **1.6.0** | `theme/kk-aurora/style.css` |
| Deploy gap | **3 versions** — 1.5.10 (#676 a11y), 1.5.11 (#680 primitives), 1.6.0 (#681 dead-CSS purge) | live vs repo |
| WordPress | **7.0.3** | homepage `generator` meta readback (docs still declare 7.0.2 — WP auto-updated) |
| Git tree | clean on `main`, in sync with origin | `git status` after fetch |
| **Git dir location** | **`/private/tmp/kriskrug-wp-git-fixed/.git` via symlink** (398M) | `ls -la .git` — **CRITICAL, see Lane 0** |
| Local-only branches | `fix-676-a11y-theme`, `pr-681`, 27 `worktree-wf_*` | `git branch` upstream scan |
| Stashes | 0 (two exported-stash `.txt` files sit in `/private/tmp`) | `git stash list` + `/private/tmp` listing |
| Newest committed report | `reports/morning-truth-20260805-*` (Aug 5) | git log on `reports/` |
| Uncommitted reports | 2 × `morning-truth-20260807-*` — **generated offline, all checks `unavailable`** | untracked files + file contents |
| Draft posts / pages | not re-verified this session (Aug 5: 66 / 4) | carry-over |

---

## What changed since 2026-08-05

- **Merges landed after the plan was cut:** #680 (primitives, 1.5.11), #681 (dead-CSS purge, 1.6.0), #691 (#685 header token fix), #692 (#688 docs versions), #693 (#686 ABSPATH guard), #695/#694 (deps), #696 (publications aurora stash restore). PR queue back to 0.
- **The deploy window never opened.** Live sat at 1.5.9 the whole time while repo moved 1.5.10 → 1.5.11 → 1.6.0. The gap is now the single biggest risk-carrying delta in the project.
- **2026-08-07 git repair session (needs KK debrief):** something broke `.git`; a repair left the real git dir in `/private/tmp/kriskrug-wp-git-fixed/` with a symlink from the repo, plus siblings `kriskrug-wp-git-original`, `kriskrug-wp-git`, `kriskrug-wp-untracked-sortout-20260807T091544`, `stash-kriskrug-wp.txt`, `stash2-kriskrug-wp.txt`. Both Aug 7 morning-truth runs happened offline and carry no live truth; they were never committed.
- **WordPress auto-updated to 7.0.3.** AGENTS.md and CURRENT-STATE still say 7.0.2.

---

## North star (ordered)

1. **Lane 0 — repo integrity rescue.** The git database evaporates on reboot. Nothing else is durable until this is done.
2. **Truth re-baseline** — fresh committed morning-truth + doc version corrections (WP 7.0.3, Aurora 1.5.9/1.6.0).
3. **Deploy window** — Aurora 1.6.0 → live (pixel-gated). Ships three queued versions at once; unblocks #601/#602 testimonials chain.
4. **Track B a11y debt** — #684 (empty `09-late.css`, invisible service titles) is the highest-priority open code bug.
5. **Track A apply wave** — carry-over from Aug 5; apply-ready artifacts still waiting on KK per-post approval.

---

## Lane 0: Repo integrity rescue (CRITICAL — do first, needs KK go-ahead)

`/Users/kk/Code/kriskrug-wp/.git` → symlink → `/private/tmp/kriskrug-wp-git-fixed/.git`. macOS clears `/private/tmp` on reboot. A restart destroys all local refs, including the two unpushed branches.

### Steps
1. Confirm no other session is mid-write (concurrent-session rule): check for `.git/index.lock` and running agents.
2. Remove the symlink; `mv /private/tmp/kriskrug-wp-git-fixed/.git /Users/kk/Code/kriskrug-wp/.git`.
3. Verify: `git status` (expect same clean state), `git fsck --no-dangling`, `git log --oneline -3`.
4. Triage `/private/tmp` siblings before they evaporate: diff `kriskrug-wp-git-original` refs against the fixed gitdir; read the two `stash-*.txt` files; salvage anything unique into the repo or `docs/current-state/reports/`.
5. Decide fate of local-only branches: `fix-676-a11y-theme` and `pr-681` look superseded by merged #676/#681 — confirm their tips are ancestors of `main` (`git merge-base --is-ancestor`), then delete or push.
6. Prune the 27 `worktree-wf_*` branches after the same ancestor proof.

### Gate
KK approval for the `mv` (state-changing) and for every branch deletion (proof-before-delete rule).

---

## Lane 1: Truth re-baseline (docs, ~30 min)

1. `make morning-truth` while online; confirm the report shows real counts (0 PRs / 44 issues / WP 7.0.3 / Aurora 1.5.9 vs 1.6.0).
2. Commit the fresh report. Decide the two offline Aug 7 reports: commit as historical record of the outage, or delete (KK call; they document the git-repair day).
3. Correct AGENTS.md **Last verified** block + CURRENT-STATE header facts: WP 7.0.3, live 1.5.9 / repo 1.6.0, gap description. (Successor to the #688 pattern.)
4. One `docs:` commit, push immediately (do not let commits sit in an at-risk gitdir — Lane 0 first if possible).

---

## Lane 2: Deploy window — Aurora 1.6.0 (EXECUTED 2026-08-10 04:42 UTC)

> **Outcome:** deployed and verified. Live readback 1.6.0; Boost re-bundled (33 focus-visible rules in, `aurora-link` and friends out); rollback preserved at `kk-aurora.bak-1786336956`. The formal pixel gate FAILed on a capture-pipeline artifact (baselines render 4000px linearized without main CSS — predates this deploy, see `issues-to-create/visual-baseline-capture-mode-mismatch.md`); verification was done via deleted-class scan of live HTML (only latent finding: dangling form-message selectors, `issues-to-create/style-css-dangling-form-selectors.md`) plus capture review of all routes. Services titles render — #684's symptom is gone on live; re-verify and close. #601/#602 can unblock pending KK read of this evidence.

Ships #676 focus-visible a11y + #680 primitives + #681 dead-CSS purge in one deploy. Bigger diff than the usual one-patch hop: 97 deleted classes + 9 keyframes means the pixel gate genuinely matters this time.

### Steps
1. `make visual-preflight` — 11/11 routes OK pre-deploy.
2. `make visual-baseline` — freeze pre-deploy live captures.
3. SFTP deploy: `python3 scripts/deploy_theme_sftp.py` (needs `WP_SFTP_PASSWORD` or Keychain `pagely-sftp-kriskrug`).
4. Public readback: `curl -s https://kriskrug.co/wp-content/themes/kk-aurora/style.css | grep Version` → expect 1.6.0.
5. `make visual-diff` — post-deploy live vs pre-deploy live; review every changed route (dead-CSS deletions are the risk surface).
6. If clean → unblock #601 (TSTM pixel-gate deploy) and #602 (testimonials page body), then #593 epic.
7. Rollback path: re-upload the 1.5.9 zip via wp-admin (see `aurora-deploy-workflow` memory) + Pagely purge.

### Gate
KK approval per step. Pixel gate compares post-deploy live vs pre-deploy live; a frozen baseline alone is not merge proof.

---

## Lane 3: Track B theme chain (after deploy)

#476 primitives and #478 dead CSS are **merged** — the Aug 5 chain moved two links. Remaining, re-sequenced:

| Issue | Status | Next step |
|---|---|---|
| #684 Empty `09-late.css` / invisible service titles (a11y, priority:high) | **Top code bug.** needs-human-review | Diagnose why the @layer fix never landed; ship as Aurora 1.6.1 candidate |
| #479 Consolidate 12 breakpoints → 3-step scale | agent:ready | Scope after #684; swarm-able |
| #481 Rename aurora-/revive-/kkm- → kk- (1.6.1) | blocked | Re-check blocker: #478 is closed; likely unblocks after deploy confirms 1.6.0 stable |
| #480 Retire Track A page-content CSS blocks (6 routes) | agent:ready | Pairs with deploy verification; the blocks are redundant post-1.6.0 |
| #690 theme.json duplicate color values | priority:low | Batch with #481 |
| #687 CHANGELOG stale (stops at 1.5.0) | priority:low | Quick win: backfill 1.5.1 → 1.6.0 from git log |
| #424 Site-wide hover/focus pass | partially done via #676 | Re-scope remainder after deploy |
| #585 Dark-neon page islands / ghost stylesheet | needs-human-review | KK design call |
| #477 Component migration epic | open | Resume one-component-per-PR after 1.6.0 is live |

---

## Lane 4: Track A apply wave (carry-over, needs KK per-artifact approval)

Unchanged from Aug 5 — none of these were applied to live WP. Statuses not re-verified against live this session.

| Issue | Artifact | WP target | Status |
|---|---|---|---|
| #4 | Alt text inventory + proposed strings | Site-wide images | Awaiting KK string review |
| #603 | Em dash remediation body | Post 12653 (AI Lands) | Apply-ready |
| #612 | Zero to One first-person body | Post 12034 | Apply-ready |
| #420 | Services copy + layout proposal | Page `services` | Needs KK design decision |
| #402 | SEO authority hubs + link matrix | Internal linking | Needs KK prioritization |
| #496 / #500 | Futureproof Festival post | New WP draft (no publish) | Verify whether draft exists, then close or create |
| #635 | Events hero backfill ship | Events pages | blocked; successor to #592 |

### Rules (unchanged)
Dry-run first → slug-match before any PATCH (INCIDENT-2026-05-15) → KK approves the exact artifact → one post per session.

---

## Lane 5: Homepage/UX + Speaking cluster (needs KK decisions)

Speaking wave is the ripest: #637 (photo rights) and #638 (taxonomy) prep merged; #638 carries `needs-decision` for KK. Then #639 (page architecture) → #640 (embed hygiene) → #641 (VideoObject schema) in wave order. Homepage set (#413–#418, #642 command board) still needs a KK design-decision session; don't start these solo.

---

## Lane 6: Hygiene (quick wins, mostly self-serve)

- Prune 27 `worktree-wf_*` branches + 2 superseded local branches (after Lane 0 ancestor proofs).
- Delete or archive `/private/tmp` repair artifacts once triaged (Lane 0 step 4).
- Remote branch sweep: ~29 `origin/*` branches with merged work (`theme/478...` upstream already gone); verify merged, then delete on origin.
- Close-or-update sweep: #642 vs stale swarm boards, #424 partial completion note.

---

## How to start a day session (paste this to the agent)

```text
Follow docs/current-state/WORK-PLAN-2026-08-09.md as the day runbook.
0) FIRST: ls -la .git — if it is still a symlink into /private/tmp, do Lane 0 (with KK) before anything else.
1) Run: make status-readonly && make env-check
2) Read the newest committed reports/morning-truth-*.md + this work plan.
3) Pick ONE lane: rescue (Lane 0), truth (Lane 1), deploy (Lane 2, needs KK), Track B (Lane 3), Track A apply (Lane 4, needs KK), or hygiene (Lane 6).
4) No live WP writes without explicit KK approval of the exact artifact/checklist.
5) One concern per commit. Push promptly — do not park commits locally while the gitdir is at risk.
```

---

## Explicitly deferred

- `.git` history rewrite / Phase C (#572: defer) — note: unrelated to the Lane 0 symlink rescue.
- Parked plugins revive (#571: keep parked).
- Homepage redesign cluster as a batch (needs KK design session).
- Full `fixes/` live reconciliation rewrite.

---

## Approval gates

| Gate | Owner |
|---|---|
| Gitdir rescue `mv` + branch deletions | KK go-ahead, proof before delete |
| Theme deploy / pixel gate | KK for each deploy step |
| Content publish / apply to live WP | KK per artifact |
| Speaking taxonomy decision (#638) | KK |
| Homepage/UX design decisions | KK per page |
