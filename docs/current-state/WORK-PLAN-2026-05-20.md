# Work Plan - 2026-05-20

> STATUS: Historical handoff. Superseded by [WORK-PLAN-2026-05-21.md](WORK-PLAN-2026-05-21.md).

**Purpose:** historical execution plan after Wave 3 implementation completion, with queue truth and stop rules.
**Snapshot time:** 2026-05-20 17:30 UTC.

## Verified State

- `main` is synced with `origin/main` (`0 0` divergence).
- Open PRs: `0`.
- Open issues: `63`.
- Open `track-b` + `aurora-v2` issues: `13`.
- Closed today on Track B:
  - `#81` via PR `#102` evidence reconciliation
  - `#84` via PR `#104` long-form lane
  - `#85` via PR `#103` inventory + PR `#105` implementation
- Still open on Track B:
  - `#86` final performance/accessibility QA gate (real staging + production-like media)

## Key Risk To Manage

`origin/aurora/v2` is currently far from `origin/main` (`18` ahead / `69` behind; large tree delta). Treat this as an intentional branch-isolation reality for now, but do not attempt broad cross-track merges until an explicit branch-integration strategy is chosen.

The placeholder `agent-pr-generator.yml` workflow is now parked as a manual, read-only diagnostic stub. Adding `auto-implement` no longer starts simulated automation or commits `.github/agent-state` files.

## Execution Order (Next Round)

Strategy reference for this round:

- `docs/current-state/AURORA-SOTA-ROADMAP-2026-05-20.md` (summer-2026 benchmark + feature sequencing for Track B)
- `docs/current-state/DIAGNOSTIC-POLISH-2026-05-20.md` (repo-truth, debt, live-fix, and workflow reconciliation)
- `docs/current-state/AURORA-MOTION-GOVERNANCE-2026-05-20.md` (motion budget and #86 QA expectations)

### 1) Run `#86` QA gate on real staging (Track B, final lane)

Scope:
- Use PR `#106` local QA packet as baseline.
- Re-run screenshots, keyboard/focus order, reduced-motion behavior, contrast samples, console/media checks, and performance notes on a staging surface with production-like media.

Evidence to produce:
- `docs/current-state/AURORA-SWARM-86-QA-2026-05-20.md`
- Updated artifacts under `docs/current-state/aurora-qa-2026-05-20/` and `docs/current-state/aurora-smoke-2026-05-20/` as needed

Verification gate before PR:
- `git diff --check`
- staging screenshot matrix (desktop/mobile for homepage + long-form + Work/Speaking)
- explicit pass/fail checklist for a11y/perf/motion/overflow/media

### 2) Docs + roadmap reconciliation (main, docs-only)

After each lane merge:
- Refresh `ISSUE-SWARM-ROADMAP-2026-05-19.md` counts with timestamped snapshots.
- Keep `AURORA-ISSUE-SWARM-2026-05-19.md` and `TOMORROW-ROADMAP-2026-05-20.md` aligned to live issue/PR state.

### 3) Track A stays queued until Track B QA gate resolves

- `#95` is no longer blocked on the retired full-site backup gate. Use dry-run, exact slug/status checks, KK review, image decision, preview QA, and rollback/delete notes for private draft work.
- Keep content lanes (`#65-#68`, `#99`) as draft-first, no production writes without explicit approval.

## Start Commands

```bash
git fetch --prune
git status --short --branch
git rev-list --left-right --count main...origin/main
git worktree list --porcelain
gh pr list --state open --limit 50
gh issue list --state open --limit 100
gh run list --limit 10
```

## Stop Rules

1. One active implementation lane at a time.
2. Never mix Track A content writes into Track B branches.
3. No issue closure without evidence artifacts.
4. Keep locked/stale external agent worktrees hands-off unless explicitly coordinated.
