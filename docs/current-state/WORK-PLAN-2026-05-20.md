# Work Plan - 2026-05-20

**Purpose:** current execution plan after Wave 3 partial completion, with queue truth and stop rules.
**Snapshot time:** 2026-05-20 04:40 UTC.

## Verified State

- `main` is synced with `origin/main` (`0 0` divergence).
- Open PRs: `0`.
- Open issues: `68`.
- Open `track-b` + `aurora-v2` issues: `14`.
- Closed today on Track B:
  - `#81` via PR `#102` evidence reconciliation
  - `#84` via PR `#104` long-form lane
- Still open on Track B:
  - `#85` restrained component library implementation
  - `#86` final performance/accessibility QA gate

## Key Risk To Manage

`origin/aurora/v2` is currently far from `origin/main` (`16` ahead / `60` behind; large tree delta). Treat this as an intentional branch-isolation reality for now, but do not attempt broad cross-track merges until an explicit branch-integration strategy is chosen.

## Execution Order (Next Round)

### 1) Finish `#85` (Track B, implementation lane)

Scope:
- Implement restrained component-library behavior in `theme/kk-aurora` only.
- Use `AURORA-COMPONENT-INVENTORY-2026-05-20.md` as the required write boundary.

Evidence to produce:
- `docs/current-state/AURORA-SWARM-85-IMPLEMENTATION-2026-05-20.md`
- `docs/current-state/aurora-smoke-2026-05-20/` screenshots and checks JSON

Verification gate before PR:
- `git diff --check`
- `jq empty theme/kk-aurora/theme.json`
- `php -l theme/kk-aurora/functions.php` (if touched)
- `node --check theme/kk-aurora/assets/js/theme.js` (if touched)

### 2) Run `#86` QA gate (Track B, only after `#85` merge)

Scope:
- Run staging-style QA focused on keyboard/focus order, reduced motion, contrast, responsive overflow, and media loading behavior.

Evidence to produce:
- `docs/current-state/AURORA-SWARM-86-QA-2026-05-20.md`
- Updated smoke artifacts under `docs/current-state/aurora-smoke-2026-05-20/`

Close/hold rule:
- Close `#86` only with explicit pass/fail checklist per criterion.

### 3) Docs + roadmap reconciliation (main, docs-only)

After each lane merge:
- Refresh `ISSUE-SWARM-ROADMAP-2026-05-19.md` counts with timestamped snapshots.
- Keep `AURORA-ISSUE-SWARM-2026-05-19.md` and `TOMORROW-ROADMAP-2026-05-20.md` aligned to live issue/PR state.

### 4) Track A stays queued until Track B QA gate resolves

- Keep `#95` blocked on fresh full-site backup gate.
- Keep content lanes (`#65-#68`, `#99`) as draft-first, no production writes without explicit approval.

## Start Commands

```bash
git fetch --prune
git status --short --branch
git rev-list --left-right --count main...origin/main
git worktree list --porcelain
gh pr list --state open --limit 50
gh issue list --state open --limit 100
```

## Stop Rules

1. One active implementation lane at a time.
2. Never mix Track A content writes into Track B branches.
3. No issue closure without evidence artifacts.
4. Keep locked/stale external agent worktrees hands-off unless explicitly coordinated.
