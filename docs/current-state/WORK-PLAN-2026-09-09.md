# Work Plan 2026-09-09: keep the front door honest, then pick one live lane

**Status:** active day runbook. **Supersedes**
[`WORK-PLAN-2026-08-25.md`](archive/WORK-PLAN-2026-08-25.md), whose issue #4
identity-repair gate and #829 apply are complete. **Plan of record:**
[`MASTER-PLAN-2026-07-30.md`](MASTER-PLAN-2026-07-30.md). Product destination:
[`CONNECTED-PORTFOLIO-PLAN.md`](CONNECTED-PORTFOLIO-PLAN.md) (#977). Declared
snapshot: [`CURRENT-STATE-2026-07-30.md`](CURRENT-STATE-2026-07-30.md)
(Makefile default; its counters are dated evidence). Use
`make status-readonly` and a public `style.css` readback for live numbers.
This plan does not authorize a live WordPress write, theme deploy, or cache
purge. Every live lane still needs explicit KK approval, a dry run, identity
verification, a private snapshot, and exact readback.

## Starting state (verified 2026-09-09)

GitHub issue state (`gh issue view`):

| Issue | State | Meaning for this runbook |
|---|---|---|
| #829 | closed | `/ai-ethics/` You Can't Drink Data card is live |
| #830 | closed | Cyber Love Garden is on `/ai-for-creatives/` |
| #831 | closed | Matt McKenna is on `/ai-conversations/` |
| #832 | closed | meetup recaps route to `/events/` |
| #833 | closed | Most Benevolent Outcomes links are live |
| #834 | **open** | remaining hub leftover; no longer blocked on #833 |

Public readback the same morning: WordPress `7.0.4`; Aurora live and repo
`main` both `1.6.11` (`make check-live-parity`). Open PR and issue counts are
volatile; do not copy them here. Re-run `make status-readonly`.

Open Studio first-proof code is in-repo: #980 / #981 / #982 are closed.
[#983](https://github.com/WalksWithASwagger/kriskrug-wp/issues/983) is the
next product gate and remains separately approval-gated. Do not publish it
from this runbook.

## Do not start

- **#830, #831, #832, or #833.** Those applies are done. Do not revive their
  payloads or treat the August 25 "begin #830" prompt as current.
- Issue #4 residuals, archive batches 4–6, `/home/`, and WCAG verification.
  They stay parked until KK names a target.
- Theme, plugin, `inc/`, security-header, XML-RPC, or Boost (#731) work.
  Each is a separate approval.

## Next live lanes (each needs its own yes)

Pick one. Do not mix them.

1. **#834** brand-navigation for the `krug ai` query. Recut against a fresh
   authenticated dry run. #833 is closed, so the old "blocked on #833" stop
   rule is stale, but the payload still shares post 11700: verify that
   identity, slug, and expected source hash before any write. No apply script
   exists yet; do not invent one in a docs session.
2. **#983** Open Studio first-practice publication prep. Human evidence,
   exact payload, snapshots, and a separate live-permission gate. Not a
   theme deploy.
3. **#4** only the exact residual KK names (page-6815, missing-attribute
   rows, or archive scope). Do not reopen the finished identity-repair set.

## How to work from this plan

1. `make doctor` then `make status-readonly`.
2. Read this file, the declared snapshot, and the one issue you have fresh
   approval to touch.
3. Stop if the issue, payload, or live HTML disagrees with this runbook.

## Exact next implementation prompt

> Do not begin #830. If KK named #834, refresh authenticated raw content for
> every shared object (including post 11700), recut the selector, dry-run,
> snapshot, and apply one object at a time. If KK named #983, stay in the
> Open Studio publication-prep lane and do not publish. Otherwise stop.

---

**Last verified:** 2026-09-09 public `style.css` + GitHub issue state for
#829–#834. Runtime counters remain readbacks, not promises.
