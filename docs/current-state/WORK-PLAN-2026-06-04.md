# Work Plan - 2026-06-04

**Run time:** 2026-06-04 04:40 UTC / 2026-06-03 21:40 PDT
**Source report:** `docs/current-state/reports/morning-truth-20260604-044048Z.md`
**Mode:** ship closeout plus read-only audit/workplan
**Production writes in this session:** published WP post `12033`, "Agent Orchestrators, Creative Insurgents & The New Stack"

## Audit Summary

- Live WordPress smoke is clean: failures `0`, warnings `0`, WordPress `6.9.4`.
- GitHub queue has open PRs `0`, open issues `65`; the declared issue count drifted from `66` to `65`.
- Draft queue remains large: draft posts `71`, draft pages `5`, future posts `0`.
- `/projects/` still redirects to `/recent-projects-include/`; the Work page OG image is nonblank.
- Homepage reveal safety net was not detected; GSAP/ScrollTrigger CDN is still detected.
- Current branch is `codex/aurora-article-lux-composition-1.3.10`, ahead of origin after the content publish commit. Treat it as a closeout branch, not the next clean work base.
- Aurora side-work risk remains: the locked `aurora/v2` worktree is divergent (`ahead 38`, `behind 50`) with modified `theme/kk-aurora.zip`. Do not merge or clean it wholesale.

## Immediate Ship Closeout

1. Push the current branch so the published-post artifacts and this audit are backed up remotely.
2. Do not open a merge PR from this branch without first checking whether the Aurora commits and Track A publishing commit should be split.
3. For the next Track A execution branch, start fresh from `origin/main` to avoid mixing content publishing with Aurora/theme history.

## P0 Human-Gated Work

- `#128` Contact-form routing and Jetpack feedback triage: continue in wp-admin/Gmail/private workflow, keep PII out of repo, and document only redacted routing evidence.
- `#95` Media appearances draft: KK editorial review is still required before publish/backlink work.
- `#75` Notion-to-WP lockdown: keep this human-reviewed because of the overwrite incident history; verify slug/ID/status guards before any update/publish runs.
- `#23` Blog category reorganization: do not bulk-edit until category intent is confirmed, because taxonomy changes affect SEO and archives.

## P1 Track A Work

- `#68` Work page canonical, OG, and proof metadata: bounded and high-value; verify canonical, OpenGraph, title/meta, `/work/` redirect, and `/recent-projects-include/`.
- `#36` and `#43` SEO/social metadata: combine into one metadata pass if the touched pages overlap; verify source HTML and public card fields.
- `#48` Accessibility statement: local draft package exists, likely a safe page-publish candidate after preview QA.
- Draft backlog: run `scripts/notion-to-wp/.venv/bin/python scripts/notion-to-wp/draft_queue_audit.py` and rank the next five candidates by readiness, not age.

## Track B / Aurora Work

- Use a separate branch or worktree from `origin/main`.
- Next likely fixes: `#125` Jetpack Boost critical CSS/performance, `#127` mobile QA, `#120` Both Hands Full proof-card overlap, `#126` Work OG image, and `#122` generic content-page polish.
- Before editing theme files, reconcile the locked `aurora/v2` worktree state or explicitly leave it as preserved evidence.

## Verification Baseline For Next Session

```bash
cd /Users/kk/Code/kriskrug-wp
git status --short --branch
git fetch --prune
make morning-truth
make verify
sed -n '1,80p' docs/current-state/reports/morning-truth-20260604-044048Z.md
```

Use the latest generated morning-truth report if a newer one exists.
