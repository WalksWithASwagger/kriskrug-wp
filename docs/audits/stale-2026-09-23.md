# Wave 5 stale docs/code scout — 2026-09-23

**Issue:** [#1063](https://github.com/WalksWithASwagger/kriskrug-wp/issues/1063)
**Prior audit:** [`stale-2026-09-22.md`](stale-2026-09-22.md) (Wave 3 / #1057)
**Waves 4 / 4b:** #1059 / PR #1060 (p1 honesty + C19 tests), #1061 / PR #1062
(p2 closed-hub banners + `docs/audits/` INDEX pointer)
**Mode:** find-only. No deletes, no renames, no refactors, no live WordPress
writes, no outbound HTTP, no secrets.
**Closed-issue evidence:** local front-door docs only
(`AGENTS.md`, `docs/current-state/WORK-PLAN-2026-09-09.md`,
`docs/current-state/CURRENT-STATE-2026-07-30.md`, dated apply receipts,
Wave 2b–4b banners). This file does not query GitHub or `kriskrug.co`.

## Counts

| Bucket | Count |
|---|---:|
| Newly found stale docs | 2 |
| Newly found likely-dead / stale code | 0 |
| Dead internal links (active surfaces) | 0 |
| TODOs/FIXMEs pointing at merged issues | 0 |
| **New findings** | **2** |
| Prior remaining (still open) | 18 |
| Wave 3 rows fixed / bannered in Waves 4 / 4b | 6 |

Severity: `p0` broken link / `p1` misleading / `p2` cleanup.
Wave 6 action: `update` | `delete` | `verify-with-human`.

Zero new findings would have been valid. This pass found two `p2` leftovers.
No new `p0` or `p1`.

---

## Method

Same shape as Waves 1 and 3, re-run after Waves 4 / 4b:

1. **Read the 2026-09-22 audit first.** Re-checked every Wave 3 ID against
   disk. Bannered / updated rows are in the delta only, not re-inventoried,
   unless the fix itself is now wrong. Sampled Wave 4 / 4b openers; they
   match the receipts. **The fixes are not wrong.**
2. **Closed-as-open scan.** Grepped agent-facing markdown for `#826`–`#833`,
   `#764`, `#767`, `#331`, `#1042`, `Begin #830`, and “prepared, not applied”.
   Compared hits to the 2026-09-09 runbook, the 2026-09-03 `#767`/`#331`
   deploy receipt, and Wave 2b–4b banners.
3. **Closed-hub drafts / SEO apply leftovers.** Read
   `issues-to-create/seo-hubs-402/` (all ten files) and
   `content/drafts/2026-08-02-seo-authority-hubs/` (`README.md`, `hub-plan.md`,
   every `fix-82*/APPLY.md`). Confirmed `#828` (`03-…`) and `#834` (`09-…`)
   are still unbannered and left them alone.
4. **INDEX pointers.** Resolved every relative link in `docs/INDEX.md` (73
   links, 0 missing). Compared displayed paths to hrefs, especially the
   May 2026 rows Wave 4b did not retouch when adding `docs/audits/`.
5. **Dead internal links.** Resolved relative markdown targets in 92 active
   files (`AGENTS.md`, `README.md`, `CONTRIBUTING.md`, `docs/` except
   `archive/` / `reports/` / `raw/`, non-archive `issues-to-create/`, and the
   plugin/script READMEs). Skipped fenced code, fragments, site-root `/…`
   routes, and `https:` URLs. **0 missing targets.**
6. **Likely-dead scripts.** Listed `scripts/*.{py,sh,js}` and mapped each
   basename against `Makefile`, `docs/`, `scripts/`, `.github/`, `content/`,
   and `fixes/`. No new spent apply helper or zero-ref script beyond the
   Wave 1 / Wave 3 remainder.
7. **TODO/FIXME.** Grepped `scripts/`, `theme/`, and `plugins/` for
   `TODO`/`FIXME` tied to issue numbers. No production hits (the only
   `scripts/` matches are still the draft-queue scanner and its fixture).

Hard gates honored: this PR adds only this file.

---

## Delta vs 2026-09-22

### Fixed / bannered (do not re-list)

Waves 4 (#1059 / `f8262b3`) and 4b (#1061 / `e8f7397`) landed. Sampled
openers; the banners and test assertions match the receipts. **The fix is
not wrong.**

| IDs | What Waves 4 / 4b did |
|---|---|
| D23 | `fixes/issue-331-apply.md` reading-note: snippet 26 activated 2026-09-03; do not re-apply |
| D24 | `SEO-ARCHIVE-INDEXABILITY-2026-08-02.md` banner + status: v2 shipped; dated measurement |
| D25 | `fix-764/README.md` opener is now “Applied 2026-08-17T05:03Z” |
| D26 | Six closed-hub child drafts (`02`, `04`–`08`) carry `#1061` applied-and-closed banners |
| D27 | `docs/INDEX.md` now points at `docs/audits/` (Wave 1 + Wave 3 files) |
| C19 | `test_apply_issue_830/831/832/833_*.py` assert “Applied and closed”; `#833` no longer requires “No live write has been made” |

`#828` (`03-…`) and `#834` (`09-…`) stay unbannered on purpose. `#828`’s
“No live write has been made” test is still correct.

### Still open (Waves 1 / 3, not re-argued)

Waves 4 / 4b left these on purpose (`verify-with-human`, or the one p1
delete that still has not been taken). Compact remainder only. Wave 5 was
told to leave C12–C15, C18, D19 and the open hub drafts `#828`/`#834`
alone; this table is inventory, not a Wave 6 start order.

| ID | Path | Sev | Wave 6 |
|---|---|---|---|
| D19 | `docs/current-state/AURORA-MOBILE-QA-127.md` | p2 | verify-with-human |
| C01–C08 | `scripts/apply_issue_{829,830,831,832,833,826,827,764}_*.py` | p1 | verify-with-human |
| C09 | `scripts/merge-swarm-safe-prs.sh` | p1 | delete |
| C10 | `scripts/deploy_events_page.py` | p1 | verify-with-human |
| C11 | `scripts/og_snippet_deploy.py` | p1 | verify-with-human |
| C12 | `scripts/category_coverage_audit.py` | p2 | verify-with-human |
| C13 | `scripts/export_press_kit_brand.py` | p2 | verify-with-human |
| C14 | `scripts/update_wp_draft_categories.py` | p2 | verify-with-human |
| C15 | `scripts/verify_wp_draft.py` | p2 | verify-with-human |
| C18 | `scripts/body_h1_migration.py` | p2 | verify-with-human |
| C20 | `scripts/add_sky_smoked_back_work_card.py` | p1 | verify-with-human |

D19 is still superseded and still at top level. C01–C08 and C20 still have
`--restore` plus unit tests; do not delete on sight. C09 is still the inert
July merge helper.

### Newly found

Listed in full in §1 below (D28–D29). No new code IDs.

---

## 1. Stale docs (new only)

### 1.1 Closed or applied work still written as open

| ID | Path | Why stale | Sev | Wave 6 |
|---|---|---|---|---|
| D28 | `content/drafts/2026-08-02-seo-authority-hubs/hub-plan.md` | Sibling `README.md` now has a 2026-09-21 execution-status table (`#826`/`#827`/`#829`–`#833` applied and closed). `hub-plan.md` has no banner and still reads as the apply queue: “That is wrong and it is the first fix” for the `#833` MBO footer, present-tense spoke tables, no “do not re-apply.” Opening this file from a child-draft Research line skips the README. `#828`/`#834` rows in the same file stay valid research. | p2 | update |

### 1.2 Wrong INDEX pointers

| ID | Path | Why stale | Sev | Wave 6 |
|---|---|---|---|---|
| D29 | `docs/INDEX.md` | D27 added `docs/audits/` and is not wrong. 23 other rows still *display* the pre-`#549` top-level path (`current-state/SWARM-STATUS-2026-05-18.md`, `current-state/ROADMAP.md`, …) while the href correctly goes to `current-state/archive/…`. Links resolve (0 missing). A cold agent copying the visible path looks in the wrong directory. Wave 2b rewrote the “Current agent-swarm lanes” wording (D11); it did not fix these labels. | p2 | update |

### 1.3 Wrong or leftover public URLs

None new on the active surface. D20–D22 banners from Wave 2c still hold.

### 1.4 Dead internal links

None found on the 92-file active surface described in Method §5.

`p0` count: **0**.

---

## 2. Likely-dead code (new only)

None. The Wave 1 / Wave 3 remainder in the delta table still covers the
spent apply helpers and unreferenced scripts. No new zero-ref top-level
script appeared after Waves 4 / 4b. C19 tests now lock the Wave 2b
“Applied and closed” strings; that fix is not wrong.

### 2.1 TODOs / FIXMEs / commented-out removed features

None found in `scripts/`, `theme/kk-aurora/`, or `plugins/` that name a
merged issue or a removed feature. Wave 6: no action.

Unused-export analysis was not run (same small Makefile-listed JS surface
as Waves 1 and 3).

---

## 3. Out of scope

Noticed, not invented as work:

- Bannered `docs/current-state/archive/WORK-PLAN-*.md` files, including
  `WORK-PLAN-2026-08-25.md` still telling a reader to start at `#830`.
- `docs/current-state/CURRENT-STATE-2026-07-30.md` counters. Declared
  snapshot; already caveated as dated evidence.
- `docs/current-state/WORK-PLAN-2026-09-09.md` — active runbook.
- Wave 2b–4b banners themselves (sampled; they match the receipts).
- Closed-hub child-draft bodies under `#1061` banners still using
  “Owns (write)” / “Blocked by.” Wave 4b chose banners, not rewrites.
  The banners are present and correct; do not re-open D26.
- Open hub drafts `#828` (`03-rewrite-negotiation-checklist.md`,
  `fix-828/APPLY.md`) and `#834` (`09-brand-navigation-krug-ai.md`).
  `#834` still lists “Blocked by” closed children 1 / 4 / 8; left alone
  on purpose. The 2026-09-09 runbook already says `#834` is no longer
  blocked on `#833`.
- `docs/current-state/reports/issue-826-apply-ready-20260817.md` and
  `issue-827-apply-ready-20260817.md` still say “Still apply-ready. Not
  applied.” Dated pre-apply receipts; `issue-826-applied-20260818.md` /
  `issue-827-applied-20260818.md` are the next-day closeouts. Same shape
  for `issue-767-apply-ready-20260817.md` vs the 2026-09-03 deploy
  receipt. `docs_truth_check.py` excludes `reports/`.
- `content/drafts/issue-996-indexing-recovery/APPLY.md`,
  `content/drafts/issue-1028-all-in-faq-aeo/APPLY.md`,
  `content/drafts/issue-1030-cross-link-matrix/APPLY.md`,
  `fixes/issue-1025-apply.md` — still correctly prep-only / not applied.
- `scripts/apply_issue_828_negotiation_checklist.py` and its
  “No live write has been made” test — `#828` is not recorded closed on
  the 2026-09-09 runbook.
- `scripts/apply_north_house_journey.py`,
  `scripts/preview_north_house_journey.py` — September packets for open
  product work.
- `docs/current-state/CONTENT-ARCHITECTURE-RESET-2026-07-01.md` still
  saying “Do not close `#122` yet.” Same undesigned-page disagreement
  Wave 3 parked. Do not invent a close/reopen.
- Dark-crystal ceremony / booth ritual drafts (hard gate).
- `scripts/archive/*` and the `#742` fossils already parked there.
- `scripts/notion-to-wp/` library modules.
- Theme, plugin, and `inc/` production code (hard gate: find-only).
- Live URL reachability, GSC, and authenticated WP reads (no outbound).
- Secret names or Varlock values.
- Adding this Wave 5 file to the `docs/INDEX.md` audits row (would be a
  second file; Wave 6, alongside D29).

---

## 4. Suggested Wave 6 order

1. **Docs, `p2`:** banner D28 so a cold agent landing on `hub-plan.md`
   cannot treat the `#833` MBO footer as the next apply. Keep `#828` /
   `#834` research rows readable.
2. **INDEX, `p2`:** D29 — prefix the 23 archived May labels with
   `archive/`, and add this Wave 5 file to the audits row.
3. **Still-human:** C01–C08, C10–C11, C20 after KK confirms rollback-only;
   delete or park C09. Leave C12–C15, C18, D19 until a human names them.

Do not start Wave 6 from this file alone. Each update or delete is its own
lane-scoped PR.
